name: test-build

on:
  pull_request:
    # paths-ignore:
    #   - '**.md'  
    branches:  
      - releases/*
  push:
    branches:
      # - main
      - releases/*
    # paths-ignore:
    #   - '**.md'
      
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
    steps:
      - uses: actions/checkout@v2
      - name: Setup node v14.18.1
        uses: actions/setup-node@v2
        with:
          node-version: v14.18.1
      - name: Run all test
        run: yarn test
      - name: Run build production
      - run: yarn build